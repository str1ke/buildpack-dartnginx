user www-data;
worker_processes 4;
error_log stderr;
pid nginx.pid;
daemon off;

events {
  worker_connections 768;
}

http {
  include mime.types;
  server {
    listen <%= ENV['PORT'] %>;
    server_name  _;
    root /app/web;
    index index.html;
  }
}

